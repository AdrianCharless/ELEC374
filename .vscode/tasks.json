{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run Verilog Simulation (Phase 1)",
      "type": "shell",
      "command": "iverilog -g2012 -o simulation_bin.out datapath.v registers/*.v memory/*.v bus/BUS.v alu/*.v tb/demo_tb.v control/*.v datapath_helpers/*.v && vvp simulation_bin.out && gtkwave waveforms.vcd ",
      "options": {
        "cwd": "${workspaceFolder}/"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": [],
      "presentation": {
        "reveal": "always",
        "panel": "shared",
        "clear": true
      }
    }
  ]
}
